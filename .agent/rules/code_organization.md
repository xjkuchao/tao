# 代码组织

- 确保项目模块化, 避免单一函数过于复杂 (建议单个函数不超过 50 行).
- 按功能分类组织代码结构, 遵循现有的 crate 划分和目录结构 (见项目结构).
- 复杂模块应拆分为多个子模块, 每个子模块职责单一.
- 公共类型集中定义在对应 crate 中, 避免散落.
- 新增编解码器实现应放在 `tao-codec` 下对应子目录中.
- 新增容器格式实现应放在 `tao-format` 下对应子目录中.
