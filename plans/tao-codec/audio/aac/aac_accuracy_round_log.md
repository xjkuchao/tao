# AAC 精度迭代 Round 日志

## 记录约定

- 每条记录对应一个子功能闭环.
- 必填字段: 子功能, 对比结论, 逻辑证据, 指标变化, 判定, 下一步.
- 证据顺序: FDK -> FFmpeg -> 规范.

## Round-0 (基建轮)

- 子功能: `infra-1`.
- 对比结论: `N/A`.
- 逻辑证据:
  - 已建立迭代计划文件.
  - 已建立 P0/P1 基线文件.
  - 已建立下载脚本与 URL->本地索引机制.
- 关键改动:
  - 新增 `coverage/download_targets.py` 用于批量下载 114 条目标样本.
  - `coverage/run_decoder.py` 新增本地优先参数(`--prefer-local-data`, `--local-root`, `--local-index`).
  - 新增 `aac_accuracy_iterative_loop.md`, `aac_accuracy_baseline.md`, `aac_accuracy_round_log.md`.
- P0 指标变化:
  - 本地优先验证命令: `python3 plans/tao-codec/audio/aac/coverage/run_decoder.py --prefer-local-data --index 1 --jobs 1 --timeout 120`.
  - 结果: 记录 `1/114` 使用 `(local)` 输入并成功写回报告.
- P1 指标变化:
  - 下载命令: `python3 plans/tao-codec/audio/aac/coverage/download_targets.py --jobs 4 --timeout 30 --retries 3`.
  - 结果: 114/114 可用, 失败 0, 本地缓存目录 `data/aac_samples`.
- 判定: `有效(基础设施完成)`.
- 下一子功能: `1-1`.

## Round-1 (P0 实测基线轮)

- 子功能: `1-1` (入口与位流同步观察).
- 对比结论: `不一致`.
- 逻辑证据:
  - 当前 `data/2.m4a` 输出存在 `lag=2048`, 且 Tao/FFmpeg 样本数为 `958464/960000`.
  - `data/1.m4a` 虽样本数对齐, 但 `max_err` 与历史文档基线相比显著升高.
- 关键改动:
  - 暂无代码改动, 仅完成 P0 实测基线更新.
- P0 指标变化:
  - `data/1.m4a`: `Tao=882000`, `FFmpeg=882000`, `max_err=2.674841955`, `psnr=32.72dB`, `精度=99.523100%`.
  - `data/2.m4a`: `Tao=958464`, `FFmpeg=960000`, `lag=2048`, `max_err=0.430480048`, `psnr=43.37dB`, `精度=99.955861%`.
- P1 指标变化: `本轮未执行全量回归`.
- 判定: `有效(定位到下一阶段主攻点)`.
- 下一子功能: `1-1` (优先排查首包裁剪与帧同步路径).

## Round-2 (1-1: 首包 priming 裁剪修复)

- 子功能: `1-1`.
- 对比结论: `不一致`.
- 逻辑证据:
  - `data/1.m4a` 首包存在 `pts=-1024` 且 ffprobe 标记 `Skip Samples=1024`.
  - `data/2.m4a` 不存在该 skip 标记, 首包 `pts=432`.
  - 固定对所有 MP4 裁剪 1024 会导致样本相关回归(`data/2.m4a` 之前出现 `lag=2048`).
- 修复改动:
  - `crates/tao-codec/src/decoders/aac/mod.rs`:
    - 改为仅在非 ADTS 且首包 `pts<0` 时推断并应用 `pending_leading_trim_samples`.
  - `crates/tao-codec/src/decoders/aac/tables.rs`:
    - 关闭固定 MP4 默认裁剪值(设为 0), 由运行时推断替代.
- P0 指标变化:
  - `data/1.m4a`: `lag` 从 `-2048` 收敛为 `0`, 样本数保持 `882000/882000`.
  - `data/2.m4a`: `lag` 从 `2048` 收敛为 `0`, 样本数从 `958464/960000` 收敛为 `960000/960000`.
- P1 指标变化: `本轮未执行全量回归`.
- 判定: `有效修复(提交候选)`.
- 下一子功能: `1-1` (继续排查频谱精度误差).

## Round-3 (1-1: 按 packet.duration 输出长度修复)

- 子功能: `1-1`.
- 对比结论: `部分一致`.
- 逻辑证据:
  - `data/1.m4a` 末包 `duration=680`, 非固定 1024.
  - 解码器输出应尊重容器 packet.duration, 避免把尾部 padding 当有效样本.
- 修复改动:
  - `crates/tao-codec/src/decoders/aac/mod.rs`:
    - 非 ADTS 路径下, 输出样本数优先使用 `packet.duration (1..=1024)`.
    - 输出字节切片改为 `[leading_trim..packet_samples]` 范围.
- P0 指标变化:
  - `data/1.m4a`: 指标保持 `lag=0`, `精度=99.523100%`.
  - `data/2.m4a`: 指标保持 `lag=0`, `精度=99.955867%`.
- P1 指标变化: `本轮未执行全量回归`.
- 判定: `有效修复(语义正确, 指标短期持平)`.
- 下一子功能: `3-2/3-3` (MS/IS/PNS 精度路径).

## Round-4 (3-2 试探修复并回滚)

- 子功能: `3-2`.
- 对比结论: `不一致(修复无效)`.
- 逻辑证据:
  - 尝试调整 `MS` 对噪声频带的跳过条件后, P0 指标无变化.
  - `data/1.m4a` 与 `data/2.m4a` 的 `max_err/psnr/精度` 全部保持不变.
- 修复改动:
  - 试探改动已回滚, 当前代码不保留该修改.
- P0 指标变化:
  - `data/1.m4a`: `lag=0`, `精度=99.523100%` (不变).
  - `data/2.m4a`: `lag=0`, `精度=99.955867%` (不变).
- P1 指标变化: `本轮未执行全量回归`.
- 判定: `无效修复(回滚)`.
- 下一子功能: `3-3` (PNS/TNS 与立体声工具链联动排查).

## Round-5 (3-3: TNS/PNS 主线修复, P0 大幅收敛)

- 子功能: `3-3`.
- 对比结论: `不一致 -> 部分一致`.
- 逻辑证据:
  - FFmpeg TNS/LPC 递推使用 `r=-coef[i]`, 与当前实现存在偏差.
  - common-window CPE 下, Tao 在 MS/IS/TNS 的落位存在可观差异, 影响过渡段稳定性.
  - PNS/pulse/tool 链路此前存在“只跳过不应用”的能力缺口.
- 修复改动:
  - `crates/tao-codec/src/decoders/aac/spectral.rs`:
    - 修正 `compute_tns_lpc` 递推符号与步骤.
    - 增加 `pulse_data` 解析与应用实现.
  - `crates/tao-codec/src/decoders/aac/mod.rs`:
    - common-window 路径改为先 MS/IS 再分别应用左右声道 TNS.
    - 输出端取消硬编码 `clamp(-1.2, 1.2)`.
  - `crates/tao-codec/src/decoders/aac/tables.rs`:
    - 维持 MP4 固定裁剪关闭, 保留运行时推断策略.
- P0 指标变化:
  - `data/1.m4a`: `99.523100% -> 99.883833%` (`max_err 2.674841955 -> 0.476204097`, `lag=0`).
  - `data/2.m4a`: `99.955867% -> 99.999998%` (`max_err 0.430480048 -> 0.000196695`, `lag=0`).
- P1 指标变化: `本轮未做全量, 先完成 P0 收敛`.
- 判定: `有效修复(保留主线)`.
- 下一子功能: `4-2` (窗口切换 + overlap-add 细节排查).

## Round-6 (4-2: 过渡段诊断增强与多组 A/B 试探)

- 子功能: `4-2`.
- 对比结论: `不一致(定位有效, 修复未收敛)`.
- 逻辑证据:
  - `data/1.m4a` 最大误差稳定在 `idx=654852`, 映射到 `channel=0, frame=327426, aac_block=319, block_offset=770`.
  - block 319 附近语法: `LONG_START -> EIGHT_SHORT -> LONG_STOP` 过渡, 且该点无 IS/PNS/TNS 直接参与.
  - 误差邻域呈连续正负摆动, 更像窗口/重叠路径局部不一致而非单点解码崩坏.
- 关键改动:
  - `plans/tao-codec/audio/aac/decoder_compare.rs`:
    - 新增最大误差映射(通道/帧/1024块/原始索引).
    - 新增按通道帧级 TopN 与按通道 1024 帧块 TopN.
  - 试探并回滚:
    - common-window 下“ICS 内即时 TNS”(退化到 `99.872666%`).
    - LONG_START/LONG_STOP 短窗方向切换(显著退化).
    - 关闭 TNS(退化到 `99.842215%`).
- P0 指标变化:
  - `data/1.m4a`: 维持 `99.883833%`.
  - `data/2.m4a`: 维持 `99.999998%`.
- P1 指标变化: `本轮未做全量`.
- 判定: `定位有效, 修复无效(回滚)`.
- 下一子功能: `4-2` 继续(过渡边界与 overlap/saved 语义对齐).

## Round-7 (P1 全量复测: retest-imprecise)

- 子功能: `P1 回归门禁`.
- 对比结论: `整体改善`.
- 执行命令:
  - `python3 plans/tao-codec/audio/aac/coverage/run_decoder.py --retest-imprecise --prefer-local-data --jobs 8`
- P1 指标变化:
  - 总计 `114`, 成功 `99`, 失败 `15`.
  - 失败编号: `9,17,33,37,46,52,54,77,79,83,89,102,103,110,111`.
  - 新增“失败->成功”共 `21` 条: `20,49,50,55,57,60,63,67,68,80,86,87,92,93,94,95,96,97,98,99,107`.
- P0 指标变化:
  - `data/1.m4a`: `99.883833%` (未过 99.9).
  - `data/2.m4a`: `99.999998%` (稳定通过).
- 判定: `有效(覆盖率与稳定性明显改善)`.
- 下一子功能: `4-2` (继续攻克 block 319 过渡段残差).

## Round 模板

- 子功能: `x-y`.
- 对比结论: `一致 | 不一致`.
- 逻辑证据:
  - FDK: `文件/函数/行为`.
  - FFmpeg: `文件/函数/行为`.
  - 规范: `条款`.
- 修复改动:
  - `...`.
- P0 指标变化:
  - `data/1.m4a`: `...`.
  - `data/2.m4a`: `...`.
- P1 指标变化: `...`.
- 判定: `有效修复(提交) | 无效修复(回滚) | 无差异(跳过)`.
- 下一子功能: `...`.

## Round-8 (4-2: 误差块原始帧映射与短块定位修正)

- 子功能: `4-2`.
- 对比结论: `定位增强(指标持平)`.
- 逻辑证据:
  - `data/1.m4a` 首包 `pts=-1024, duration=1024`, Tao 会执行 `priming` 裁剪 1024 样本.
  - 旧日志中的 `aac_block=319` 为“裁剪后块索引”, 易误判到 `LONG_START`.
  - 新增原始帧映射后确认最大误差点对应 `raw_aac_block=320, raw_block_offset=770` (short 相关块).
- 修复改动:
  - `plans/tao-codec/audio/aac/decoder_compare.rs`:
    - `decode_aac_with_tao` 新增 `estimated_leading_trim_samples` 输出.
    - 最大误差打印新增“原始帧映射(含priming)”字段, 同时给出 `raw_frame/raw_time/raw_aac_block/raw_block_offset`.
- P0 指标变化:
  - `data/1.m4a`: 维持 `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`.
  - `data/2.m4a`: 维持 `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`.
- P1 指标变化: `本轮未执行全量`.
- 判定: `有效修复(诊断能力增强, 纠正定位偏差)`.
- 下一子功能: `4-2` 继续(围绕 raw block 320 的 short/overlap 链路做实现级对齐).

## Round-9 (4-2: short 热点块工具链定位与回归试探)

- 子功能: `4-2`.
- 对比结论: `定位增强(实现试探未收敛)`.
- 逻辑证据:
  - 通过块级诊断确认 `raw block=320` 的语法特征为:
    - `win_seq=EIGHT_SHORT_SEQUENCE`, `groups=4`, `max_sfb=12`.
    - 同时存在 `PNS` 与 `TNS`(左右声道均有 1 个 short TNS 滤波器, `w2: len=9, order=7, dir=false`).
  - 邻接块语法:
    - `block=319`: `LONG_START`, 无 `IS/PNS/TNS`.
    - `block=321`: `LONG_STOP`, 无 `IS/PNS/TNS`.
  - 参考实现核对:
    - FFmpeg `aacdec_proc_template.c` 的 PNS 重建路径与当前实现在“随机源+频带归一化”层面对齐.
    - FFmpeg `aacdec_dsp_template.c` 的 TNS 频带起点逻辑仍以 `num_swb` 起算并做 `mmm` 截断.
- 试探改动与结果:
  - 试探 A: short TNS 频带起点改为 `mmm` 起算(仅 short).
    - 结果: `data/1.m4a` 退化到 `99.877585%`, `max_err=0.629226506`.
  - 试探 B: 关闭 short TNS.
    - 结果: 退化到 `99.845697%`, `max_err=0.790722549`.
  - 试探 C: PNS 幅度改为 `-2^(sf/4)`.
    - 结果: 严重退化到 `43.914719%`, `lag=-4`.
  - 试探 D: short PNS 关闭 / 仅翻转符号.
    - 结果: 指标基本不变, 对主误差无收益.
  - 试探 E: 无符号码本 sign-bit 读取顺序反转.
    - 结果: 严重退化到 `80.639557%`, `max_err=2.774318278`.
  - 临时块级 `AAC-DBG` 诊断代码已全部回滚, 主线不保留调试分支.
- P0 指标变化:
  - `data/1.m4a`: 最终回到 `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0`.
  - `data/2.m4a`: 维持 `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`, `lag=0`.
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(全部回滚), 但定位信息有效`.
- 下一子功能: `4-2` 继续(聚焦 short 块内 TNS 作用窗与频带映射的位级对齐).

## Round-10 (4-2: short overlap/saved 语义 A/B 与回滚)

- 子功能: `4-2`.
- 对比结论: `不一致(实现试探未收敛)`.
- 逻辑证据:
  - 参考核对(FFmpeg `aacdec_dsp_template.c` + `float_dsp.c`)显示:
    - short 路径核心是 `vector_fmul_window` 的 pairwise overlap.
    - `saved` 更新按 `window_sequence` 分支, 非统一尾段切片.
  - 当前 Tao 仍采用“short 每块独立加窗累加 + 统一 `windowed[1024..2048]` 保存”的简化语义.
- 试探改动与结果:
  - 试探 A: `LONG_START` 后继 overlap 改为 IMDCT 中段/尾段保存(仅 `mod.rs`).
    - 结果: `data/1.m4a` 退化到 `95.362528%`(`max_err=1.709018111`), `data/2.m4a` 退化到 `89.051385%`(`max_err=1.627628803`).
  - 试探 B: `ONLY_LONG` 分支全程使用 `prev` 窗.
    - 结果: P0 指标无变化.
  - 试探 C: `synthesize_short_windows` 改为显式 pairwise overlap 拼接.
    - 结果: `data/1.m4a` 退化到 `91.183297%`(`max_err=1.232931618`), `data/2.m4a` 退化到 `93.115556%`(`max_err=0.971358508`).
  - 上述试探均已完全回滚, 主线不保留实验逻辑.
- P0 指标变化:
  - `data/1.m4a`: 最终回到 `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0`.
  - `data/2.m4a`: 最终回到 `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`, `lag=0`.
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(全部回滚), 但证据强化`.
- 下一子功能: `4-2` 继续(需要在不破坏现有链路前提下, 原型化完整的 FFmpeg 风格 short pairwise + saved 状态机).

## Round-11 (4-2: PNS 标定与 Huffman 隐式失败假设验证)

- 子功能: `4-2`.
- 对比结论: `不一致(假设不成立, 实现试探无收益)`.
- 逻辑证据:
  - 假设 A: short 热点可能来自 PNS 目标幅度基准常数偏差.
  - 假设 B: short/long 频谱解码中存在“静默 Huffman 失败后截断”, 导致局部残差.
- 试探改动与结果:
  - 试探 A: PNS 目标幅度由 `2^((sf-100)/4)` 临时改为 `2^((sf-120)/4)`.
    - 结果: 指标仅出现浮点微扰, `data/1.m4a` 仍为 `99.883833%` 量级, 主误差位置与量级不变.
  - 试探 B: 临时将 short/long Huffman `decode_values` 失败从“静默截断”改为“直接报错”.
    - 结果: `data/1.m4a`、`data/2.m4a` 全流程未触发任何该类报错, 指标与基线一致.
  - 结论: 当前 P0 热点并非由“隐藏 Huffman 截断”驱动; PNS 常数本轮假设未带来有效收敛.
  - 上述改动均已回滚, 主线不保留.
- P0 指标变化:
  - `data/1.m4a`: 回到 `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0`.
  - `data/2.m4a`: 回到 `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`, `lag=0`.
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(全部回滚), 但排除两项高疑点`.
- 下一子功能: `4-2` 继续(转向“长块尾段->short 首段”的保存状态机一致性验证).

## Round-12 (4-2: short saved 偏移窗口试探与回滚)

- 子功能: `4-2`.
- 对比结论: `不一致(实现试探显著退化)`.
- 逻辑证据:
  - 假设: 当前 short 块保存区可能存在固定偏移, 导致 short->long 过渡误差.
  - 试探方案: 仅在 `EIGHT_SHORT_SEQUENCE` 下, overlap 保存窗口从 `[1024..2048]` 改为 `[960..1984]`.
- 试探改动与结果:
  - `crates/tao-codec/src/decoders/aac/mod.rs`:
    - 临时修改 short 序列 overlap 保存切片.
  - 回归结果:
    - `data/1.m4a` 退化到 `78.695222%` (`max_err=2.069433451`, `psnr=15.20dB`).
    - `data/2.m4a` 退化到 `89.864838%` (`max_err=1.844016075`, `psnr=19.30dB`).
  - 判定为强退化, 已完整回滚.
- P0 指标变化:
  - `data/1.m4a`: 回到 `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0`.
  - `data/2.m4a`: 回到 `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`, `lag=0`.
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(回滚), 但排除“固定偏移保存窗口”路径`.
- 下一子功能: `4-2` 继续(转向按窗口序列重构 FFmpeg 式 saved 状态机原型).

## Round-13 (4-2: 过渡分支化混合试探 + CCE 排除诊断)

- 子功能: `4-2`.
- 对比结论: `不一致(实现试探无收益, 假设排除有效)`.
- 逻辑证据:
  - 试探 A: 在 `decode_raw_frame` 中引入 `prev_window_sequence` 并按 FFmpeg `imdct_and_windowing` 的 long-to-long/过渡分支切换混合策略.
  - 结果: `data/1.m4a` 与 `data/2.m4a` 指标完全不变, 说明当前残差并非由该层混合分支直接驱动.
  - 试探 B: 增加临时开关 `TAO_AAC_FAIL_ON_CCE` 做“命中 CCE 即失败”探针.
  - 结果: `data/1.m4a` 未出现任何“检测到 CCE”命中, 且未触发额外解码失败日志, 可排除 CCE 缺失是当前 P0 主残差根因.
  - 进一步核对: FFmpeg `aactab.c` 的 `codebook_vector_idx` 元数据与 Tao 当前线性索引值映射在 CB7/8/9/10/11 上口径一致, 未发现直接错位证据.
- 试探改动与结果:
  - `crates/tao-codec/src/decoders/aac/mod.rs`:
    - 临时加入窗口序列分支化混合逻辑, P0 指标无变化后已回滚.
    - 临时加入 `TAO_AAC_FAIL_ON_CCE` 探针, 完成诊断后已回滚.
- P0 指标变化:
  - `data/1.m4a`: `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0` (保持不变).
  - `data/2.m4a`: `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`, `lag=0` (保持不变).
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(回滚), 但完成关键假设排除`.
- 下一子功能: `4-2` 继续(转向 IMDCT/窗函数实现级对齐, 优先验证 short/long 过渡块的时域构形差异来源).

## Round-14 (4-2: short 右半窗索引镜像试探 + 码本映射复核)

- 子功能: `4-2`.
- 对比结论: `不一致(试探无收益, 诊断结论维持)`.
- 逻辑证据:
  - 试探 A: 将 `synthesize_short_windows` 右半窗权重从 `short_curr[i]` 临时改为镜像索引 `short_curr[255-i]`.
  - 结果: `data/1.m4a` 与 `data/2.m4a` 指标完全不变, 说明该索引方向并非当前残差决定因素.
  - 诊断 B: 复核 FFmpeg `aactab.c` 的 `codebook_vector02_idx/4_idx/6_idx/8_idx/10_idx` 打包规则与 Tao 当前线性映射关系.
  - 结果: CB1-11 映射口径均未发现不一致证据, 暂不支持“码本索引值映射错误”假设.
- 试探改动与结果:
  - `crates/tao-codec/src/decoders/aac/imdct.rs`:
    - 临时修改 short 右半窗索引方式.
    - P0 指标无变化后已回滚.
- P0 指标变化:
  - `data/1.m4a`: `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0` (保持不变).
  - `data/2.m4a`: `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`, `lag=0` (保持不变).
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(回滚), 但诊断范围收敛`.
- 下一子功能: `4-2` 继续(转向 IMDCT 长块 overlap 核心公式的最小替换试探).

## Round-15 (4-2: 最大误差点局部 lag 诊断增强)

- 子功能: `4-2`.
- 对比结论: `定位增强(主假设排除)`.
- 逻辑证据:
  - 在 `decoder_compare.rs` 增加 `local_best_lag_by_channel` 诊断, 对最大误差点做局部对齐搜索.
  - `data/1.m4a` 结果: `local_lag=0`, `global_lag=0`, `combined_lag=0`.
  - 结论: 当前主热点并非“局部时移错位”导致, 可继续收敛到窗口/频谱路径.
- 试探改动与结果:
  - `plans/tao-codec/audio/aac/decoder_compare.rs`:
    - 新增 `local_best_lag_by_channel(...)`.
    - 在 `run_compare` 输出最大误差点局部 lag 诊断信息.
- P0 指标变化:
  - `data/1.m4a`: `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0` (保持不变).
  - `data/2.m4a`: `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`, `lag=0` (保持不变).
- P1 指标变化: `本轮未执行全量`.
- 判定: `有效修复(诊断能力增强)`.
- 下一子功能: `4-2` 继续(保持单点最小试探).

## Round-16 (4-2: short 首窗右半形状对齐试探)

- 子功能: `4-2`.
- 对比结论: `不一致(试探无收益)`.
- 逻辑证据:
  - 试探: `synthesize_short_windows` 中首个 short 窗(`win=0`)右半窗形状由固定 `curr` 改为与左半一致(`prev`).
  - 回归结果: `data/1.m4a` 与 `data/2.m4a` 指标完全不变.
  - 结论: 首窗右半形状选择不是当前 P0 残差决定因素.
- 试探改动与结果:
  - `crates/tao-codec/src/decoders/aac/imdct.rs`:
    - 临时引入 `short_right` 分支.
    - P0 无变化后已完整回滚.
- P0 指标变化:
  - `data/1.m4a`: `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0` (保持不变).
  - `data/2.m4a`: `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`, `lag=0` (保持不变).
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(回滚)`.
- 下一子功能: `4-2` 继续(转向工具链顺序与过渡块语义).

## Round-17 (4-2: common-window short 即时 TNS 细粒度试探)

- 子功能: `4-2`.
- 对比结论: `不一致(实现试探退化)`.
- 逻辑证据:
  - 试探: 仅在 `common_window && EIGHT_SHORT_SEQUENCE` 下恢复 ICS 内即时 TNS, long 序列仍保持延后 TNS.
  - 回归结果:
    - `data/1.m4a` 退化到 `99.874035%` (`max_err=0.546605170`).
    - `data/2.m4a` 退化到 `99.986437%` (`max_err=0.574212044`, `psnr=48.50dB`).
  - 结论: 该顺序调整会破坏当前已稳定路径, 不应保留.
- 试探改动与结果:
  - `crates/tao-codec/src/decoders/aac/mod.rs`:
    - 临时加入 short/common-window 即时 TNS 分支与解析路径切换.
    - 出现退化后已完整回滚.
- P0 指标变化:
  - 回滚后恢复:
    - `data/1.m4a`: `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0`.
    - `data/2.m4a`: `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`, `lag=0`.
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(回滚), 但顺序假设收敛`.
- 下一子功能: `4-2` 继续(转向 long/short 交界窗表与 KBD 参数路径核对).

## Round-18 (4-2: PNS 随机序列探针 + 多热点复核)

- 子功能: `4-2`.
- 对比结论: `不一致(探针无收益, 定位范围扩展)`.
- 逻辑证据:
  - 全量诊断输出显示 `data/1.m4a` 不仅在 block=319 有热点, 还存在 `block=132/239/11/104` 的高误差块.
  - 试探: 在 `decode_raw_frame` 临时加入“每帧重置 PNS 随机种子”.
  - 回归结果几乎不变:
    - `data/1.m4a` 仅出现浮点微扰(`max_err=0.476204693`), 其余指标不变.
    - `data/2.m4a` 指标保持不变.
  - 结论: 当前主残差不由“PNS 种子跨帧漂移”驱动.
- 试探改动与结果:
  - `crates/tao-codec/src/decoders/aac/mod.rs`:
    - 临时按帧重置 `random_state`.
    - 无收益后已完整回滚.
  - `plans/tao-codec/audio/aac/decoder_compare.rs` 输出复核:
    - `local_lag=0`, `combined_lag=0` 继续成立.
    - block 级热点扩展到多处, 需避免只盯单一过渡块.
- P0 指标变化:
  - 回滚后恢复:
    - `data/1.m4a`: `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0`.
    - `data/2.m4a`: `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`, `lag=0`.
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(回滚), 但完成假设排除与热点扩展定位`.
- 下一子功能: `4-2` 继续(转向 IMDCT 基函数缩放/窗函数叠加的实现口径核对).

## Round-19 (4-2: pulse tool 贡献度探针)

- 子功能: `4-2`.
- 对比结论: `不一致(探针无收益)`.
- 逻辑证据:
  - 试探: 保持 `pulse_data` 解析不变, 临时跳过 `apply_pulse_data`.
  - 回归结果:
    - `data/1.m4a` 指标完全不变(`99.883833%`, `max_err=0.476204097`).
    - `data/2.m4a` 指标完全不变(`99.999998%`, `max_err=0.000196695`).
  - 结论: 当前 P0 主残差与 pulse 应用无直接相关性.
- 试探改动与结果:
  - `crates/tao-codec/src/decoders/aac/mod.rs`:
    - 临时移除 `apply_pulse_data` 调用.
    - 无收益后已完整回滚.
- P0 指标变化:
  - 回滚后保持:
    - `data/1.m4a`: `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0`.
    - `data/2.m4a`: `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`, `lag=0`.
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(回滚), 但完成 pulse 假设排除`.
- 下一子功能: `4-2` 继续(转向立体声工具链细分定位).

## Round-20 (4-2: IS 分支参与度探针)

- 子功能: `4-2`.
- 对比结论: `不一致(试探退化, 说明 IS 仍是有效链路)`.
- 逻辑证据:
  - 试探: 在 common-window 路径临时跳过 `apply_intensity_stereo`.
  - 回归结果:
    - `data/1.m4a` 精度由 `99.883833%` 退化到 `99.826215%`, `psnr` 降至 `37.12dB`.
    - `data/2.m4a` 指标基本不变.
  - 结论: IS 分支对 `data/1.m4a` 有正向贡献, 当前实现不应移除.
- 试探改动与结果:
  - `crates/tao-codec/src/decoders/aac/mod.rs`:
    - 临时移除 `apply_intensity_stereo` 调用.
    - 出现退化后已完整回滚.
- P0 指标变化:
  - 回滚后恢复:
    - `data/1.m4a`: `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0`.
    - `data/2.m4a`: `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`, `lag=0`.
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(回滚), 但确认 IS 路径需保留`.
- 下一子功能: `4-2` 继续(转向 IS 标度/符号细节与 FFmpeg 口径核对).

## Round-21 (4-2: IS 指数方向探针)

- 子功能: `4-2`.
- 对比结论: `不一致(试探退化)`.
- 逻辑证据:
  - 试探: `apply_intensity_stereo` 标度从 `0.5^(is/4)` 临时改为 `2^(is/4)`.
  - 回归结果:
    - `data/1.m4a`: `精度 99.883833% -> 99.877792%`, `psnr 38.87dB -> 38.65dB`.
    - `data/2.m4a`: 指标维持稳定.
  - 结论: 当前 `0.5^(is/4)` 方向更接近参考实现, 不应翻转指数方向.
- 试探改动与结果:
  - `crates/tao-codec/src/decoders/aac/spectral.rs`:
    - 临时修改 IS 标度公式.
    - 出现退化后已完整回滚.
- P0 指标变化:
  - 回滚后恢复:
    - `data/1.m4a`: `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0`.
    - `data/2.m4a`: `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`, `lag=0`.
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(回滚), 但确认 IS 指数方向`.
- 下一子功能: `4-2` 继续(转向 IS 标度常量与 ms_used 符号路径细节探针).

## Round-22 (4-2: ms_used 对 IS 符号翻转探针)

- 子功能: `4-2`.
- 对比结论: `不一致(试探轻微退化)`.
- 逻辑证据:
  - 试探: 临时移除 `ms_used` 对 IS `sign` 的翻转逻辑.
  - 回归结果:
    - `data/1.m4a`: `精度 99.883833% -> 99.883545%`, `psnr 38.87dB -> 38.86dB`.
    - `data/2.m4a`: 指标保持稳定.
  - 结论: `ms_used` 符号翻转在当前实现中是必要路径, 不应移除.
- 试探改动与结果:
  - `crates/tao-codec/src/decoders/aac/spectral.rs`:
    - 临时去掉 `ms_used` 触发的 `sign=-sign`.
    - 出现退化后已完整回滚.
- P0 指标变化:
  - 回滚后恢复:
    - `data/1.m4a`: `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0`.
    - `data/2.m4a`: `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`, `lag=0`.
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(回滚), 但确认 ms_used 符号分支`.
- 下一子功能: `4-2` 继续(转向 IS 量化位置口径与 short group 边界核对).

## Round-23 (4-2: MS Stereo 参与度探针)

- 子功能: `4-2`.
- 对比结论: `不一致(试探强退化)`.
- 逻辑证据:
  - 试探: 在 common-window 路径临时跳过 `apply_ms_stereo`.
  - 回归结果:
    - `data/1.m4a` 退化到 `70.833835%` (`max_err=1.392444535`, `psnr=13.38dB`).
    - `data/2.m4a` 退化到 `64.334505%` (`max_err=1.384822983`, `psnr=12.39dB`, `lag=1`).
  - 结论: MS Stereo 是主链路关键能力, 当前实现必须保留.
- 试探改动与结果:
  - `crates/tao-codec/src/decoders/aac/mod.rs`:
    - 临时移除 `apply_ms_stereo` 调用.
    - 出现强退化后已完整回滚.
- P0 指标变化:
  - 回滚后恢复:
    - `data/1.m4a`: `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0`.
    - `data/2.m4a`: `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`, `lag=0`.
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(回滚), 但确认 MS 路径为高优先保留`.
- 下一子功能: `4-2` 继续(转向 MS 数值缩放与 band 过滤口径探针).

## Round-24 (4-2: MS 数值缩放 0.5 探针)

- 子功能: `4-2`.
- 对比结论: `不一致(试探强退化)`.
- 逻辑证据:
  - 试探: 将 `apply_ms_stereo` 从 `L/R = (L±R)` 临时改为 `0.5*(L±R)`.
  - 回归结果:
    - `data/1.m4a` 退化到 `84.013243%` (`max_err=0.910876483`, `psnr=16.73dB`).
    - `data/2.m4a` 退化到 `80.682942%` (`max_err=0.706318974`, `psnr=16.03dB`).
  - 结论: 当前 MS 口径不应引入额外 0.5 缩放.
- 试探改动与结果:
  - `crates/tao-codec/src/decoders/aac/spectral.rs`:
    - 临时加入 `0.5` 系数.
    - 出现强退化后已完整回滚.
- P0 指标变化:
  - 回滚后恢复:
    - `data/1.m4a`: `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0`.
    - `data/2.m4a`: `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`, `lag=0`.
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(回滚), 但确认 MS 幅度口径`.
- 下一子功能: `4-2` 继续(转向 M/S 频带过滤条件与 short group 边界核对).

## Round-25 (4-2: MS band_type 过滤放开探针)

- 子功能: `4-2`.
- 对比结论: `无差异(试探无收益)`.
- 逻辑证据:
  - 试探: `apply_ms_stereo` 调用临时取消 `left/right band_types` 过滤(传 `None`).
  - 回归结果: `data/1.m4a` 与 `data/2.m4a` 指标完全不变.
  - 结论: 当前 P0 残差不由 MS band_type 过滤条件驱动.
- 试探改动与结果:
  - `crates/tao-codec/src/decoders/aac/mod.rs`:
    - 临时放开 MS 过滤条件.
    - 无差异后已完整回滚.
- P0 指标变化:
  - `data/1.m4a`: `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0` (不变).
  - `data/2.m4a`: `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`, `lag=0` (不变).
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(回滚), 但排除 MS 过滤条件假设`.
- 下一子功能: `4-2` 继续(转向工具出现性诊断).

## Round-26 (4-2: gain_control_data 出现性探针)

- 子功能: `4-2`.
- 对比结论: `无差异(假设排除)`.
- 逻辑证据:
  - 试探: 增加 `TAO_AAC_FAIL_ON_GAIN_CONTROL` 探针, 一旦出现 `gain_control_data` 立即报错.
  - 在 `data/1.m4a` 执行探针命令后未命中该错误, 仍为原精度门限失败.
  - 结论: 当前 P0 残差不是由 `gain_control_data` 未实现导致.
- 试探改动与结果:
  - `crates/tao-codec/src/decoders/aac/mod.rs`:
    - 临时加入 gain_control 出现性探针.
    - 验证后已完整回滚.
- P0 指标变化:
  - `data/1.m4a`: 维持 `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0`.
  - `data/2.m4a`: 本轮未重跑(主线近期稳定, 参考上一轮基线).
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(回滚), 但完成 gain_control 假设排除`.
- 下一子功能: `4-2` 继续(转向 pulse_data 出现性与作用范围诊断).

## Round-27 (4-2: pulse_data 出现性探针)

- 子功能: `4-2`.
- 对比结论: `无差异(假设排除)`.
- 逻辑证据:
  - 试探: 增加 `TAO_AAC_FAIL_ON_PULSE` 探针, 一旦出现 `pulse_data` 立即报错.
  - 在 `data/1.m4a` 执行探针命令后未命中该错误, 仍为原精度门限失败.
  - 与 Round-19 的“跳过 pulse 应用无影响”结论一致.
  - 结论: 当前 P0 残差不是由 `pulse_data` 解析/应用路径驱动.
- 试探改动与结果:
  - `crates/tao-codec/src/decoders/aac/mod.rs`:
    - 临时加入 pulse 出现性探针.
    - 验证后已完整回滚.
- P0 指标变化:
  - `data/1.m4a`: `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0` (不变).
  - `data/2.m4a`: 本轮未重跑(近期基线稳定).
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(回滚), 但完成 pulse 假设排除`.
- 下一子功能: `4-2` 继续(转向 TNS 滤波符号与递推口径复核).

## Round-28 (4-2: TNS 滤波符号探针)

- 子功能: `4-2`.
- 对比结论: `不一致(试探灾难性退化)`.
- 逻辑证据:
  - 试探: `apply_tns_data` 中滤波递推由 `acc -= src*lpc` 临时改为 `acc += src*lpc`.
  - 回归结果出现数值崩坏:
    - `data/1.m4a`: `max_err=607776.115838438`, `psnr=-74.27dB`.
    - `data/2.m4a`: `max_err=98307472078405632`, `psnr=-294.91dB`, `lag=-3882`.
  - 结论: 当前 TNS 滤波符号不能翻转, 现有实现符号方向正确.
- 试探改动与结果:
  - `crates/tao-codec/src/decoders/aac/spectral.rs`:
    - 临时修改 TNS 递推符号.
    - 强退化后已完整回滚.
- P0 指标变化:
  - 回滚后确认恢复:
    - `data/2.m4a`: `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`, `lag=0`.
    - `data/1.m4a`: 回到既有基线 `99.883833%` 口径.
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(回滚), 但确认 TNS 递推符号方向`.
- 下一子功能: `4-2` 继续(转向 TNS 作用频带边界与窗组索引细节探针).

## Round-29 (4-2: short 全局振幅扫描探针)

- 子功能: `4-2`.
- 对比结论: `无差异(假设排除)`.
- 逻辑证据:
  - 试探: 在 short 合成路径增加临时参数 `TAO_AAC_SHORT_SCALE` 并扫描 `0.92~1.08`.
  - 扫描结果: `scale=1.00` 时 `psnr/精度` 最高(`99.883833%`), 其余值均下降.
  - 结论: 当前残差不是 short 全局振幅系数偏差导致.
- 试探改动与结果:
  - `crates/tao-codec/src/decoders/aac/imdct.rs`:
    - 临时加入 short scale 探针.
    - 扫描后已完整回滚.
- P0 指标变化:
  - 回滚后恢复:
    - `data/1.m4a`: `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0`.
    - `data/2.m4a`: 维持既有基线 `99.999998%`.
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(回滚), 但完成 short 振幅假设排除`.
- 下一子功能: `4-2` 继续(转向 long 路径振幅与窗后合成口径扫描).

## Round-30 (4-2: long 全局振幅扫描探针)

- 子功能: `4-2`.
- 对比结论: `无差异(假设排除)`.
- 逻辑证据:
  - 试探: 在 long 窗后路径增加临时参数 `TAO_AAC_LONG_SCALE` 并扫描 `0.96~1.04`.
  - 扫描结果: `LONG_SCALE=1.00` 时 `psnr/精度/相关系数` 最优, 偏离后均下降.
  - 结论: 当前残差不是 long 全局振幅系数偏差导致.
- 试探改动与结果:
  - `crates/tao-codec/src/decoders/aac/imdct.rs`:
    - 临时加入 long scale 探针.
    - 扫描后已完整回滚.
- P0 指标变化:
  - 回滚后恢复:
    - `data/1.m4a`: `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0`.
    - `data/2.m4a`: 维持 `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`, `lag=0`.
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(回滚), 但完成 long 振幅假设排除`.
- 下一子功能: `4-2` 继续(转向非线性/局部工具链误差源定位).

## Round-31 (4-2: 幅值分桶误差诊断增强)

- 子功能: `4-2`.
- 对比结论: `定位增强`.
- 逻辑证据:
  - 在 `decoder_compare.rs` 增加“按参考幅值分桶误差统计”.
  - `data/1.m4a` 结果显示:
    - 中等幅值区间 `abs_ref (0.50,1.00]` 同时出现较高 `mse/max_err`.
    - 高幅值 `>1.0` 样本数量很少(2553), 说明误差并非单纯“极值剪裁”问题.
  - 结论: 误差更像局部工具链/过渡块系统性偏差, 非全局增益问题.
- 试探改动与结果:
  - `plans/tao-codec/audio/aac/decoder_compare.rs`:
    - 新增 `error_bins_by_ref_abs(...)`.
    - 在低精度路径输出分桶统计.
- P0 指标变化:
  - `data/1.m4a`: 指标保持 `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0`.
  - `data/2.m4a`: 本轮未重跑(近期基线稳定).
- P1 指标变化: `本轮未执行全量`.
- 判定: `有效修复(诊断能力增强)`.
- 下一子功能: `4-2` 继续(转向线性比例/偏置可解释性诊断).

## Round-32 (4-2: 线性比例/偏置可解释性诊断)

- 子功能: `4-2`.
- 对比结论: `定位增强`.
- 逻辑证据:
  - 新增线性校正诊断: `test' = gain*test + bias`.
  - `data/1.m4a` 诊断结果:
    - `gain=1.000136092`, `bias=0.000000017`.
    - 线性校正后估计精度 `99.883835%`, 与原始 `99.883833%` 几乎一致.
  - 结论: 误差不可由简单线性比例/偏置修正解释, 根因在局部工具链语义而非全局线性失配.
- 试探改动与结果:
  - `plans/tao-codec/audio/aac/decoder_compare.rs`:
    - 新增 `fit_linear_adjustment(...)`.
    - 在低精度路径输出线性校正诊断行.
- P0 指标变化:
  - `data/1.m4a`: 指标保持 `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0`.
- P1 指标变化: `本轮未执行全量`.
- 判定: `有效修复(诊断能力增强)`.
- 下一子功能: `4-2` 继续(转向 common-window 下 TNS 时序细粒度试探).

## Round-33 (4-2: common-window 下 long 即时 TNS 试探)

- 子功能: `4-2`.
- 对比结论: `不一致(轻微退化)`.
- 逻辑证据:
  - 试探: common-window 下改为“仅 short 延后 TNS, long 在 ICS 内即时 TNS”.
  - 回归结果:
    - `data/1.m4a`: `精度 99.883833% -> 99.882463%`, `psnr 38.87dB -> 38.82dB`.
    - `data/2.m4a`: 指标保持稳定.
  - 结论: 当前“common-window 统一延后 TNS”仍更优.
- 试探改动与结果:
  - `crates/tao-codec/src/decoders/aac/mod.rs`:
    - 临时增加 long/short 分流的 TNS 时序.
    - 轻微退化后已完整回滚.
- P0 指标变化:
  - 回滚后恢复:
    - `data/1.m4a`: `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0`.
    - `data/2.m4a`: `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`, `lag=0`.
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(回滚), 但收敛 TNS 时序假设`.
- 下一子功能: `4-2` 继续(转向 short overlap/save 状态机的精细差分诊断).

## Round-34 (4-2: PNS sf 偏移参数扫描)

- 子功能: `4-2`.
- 对比结论: `无差异(假设排除)`.
- 逻辑证据:
  - 试探: 增加临时参数 `TAO_AAC_PNS_SF_SHIFT` 扫描 `-8..8`.
  - 结果: `psnr/精度` 基本不变, 仅有浮点级 `max_err` 微扰.
  - 结论: PNS 目标幅值 `sf` 偏移不是当前 P0 残差主因.
- 试探改动与结果:
  - `crates/tao-codec/src/decoders/aac/spectral.rs`:
    - 临时加入 PNS sf 偏移探针.
    - 扫描后已完整回滚.
- P0 指标变化:
  - 回滚后保持:
    - `data/1.m4a`: `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0`.
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(回滚), 但完成 PNS 假设排除`.
- 下一子功能: `4-2` 继续(转向 IS 局部幅值口径扫描).

## Round-35 (4-2: IS 幅值 bias 参数扫描)

- 子功能: `4-2`.
- 对比结论: `无差异(假设排除)`.
- 逻辑证据:
  - 试探: 增加临时参数 `TAO_AAC_IS_SCALE_BIAS` 扫描 `-0.50..0.50`.
  - 结果: `max_err` 不变, `psnr/精度` 在 `bias=0.00` 时最优.
  - 结论: Intensity Stereo 全局幅值偏移不是当前残差主因.
- 试探改动与结果:
  - `crates/tao-codec/src/decoders/aac/spectral.rs`:
    - 临时加入 IS bias 探针.
    - 扫描后已完整回滚.
- P0 指标变化:
  - 回滚后保持:
    - `data/1.m4a`: `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0`.
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(回滚), 但完成 IS 幅值偏置假设排除`.
- 下一子功能: `4-2` 继续(转向 TNS 频带上限边界细节试探).

## Round-36 (4-2: long 序列 TNS band_limit 试探)

- 子功能: `4-2`.
- 对比结论: `不一致(轻微退化)`.
- 逻辑证据:
  - 试探: 仅 long 序列下使用 `num_swb` 替代 `tns_max_bands` 截断(环境变量探针).
  - 结果:
    - `data/1.m4a`: `99.883833% -> 99.883627%` (轻微退化).
    - `data/2.m4a`: 指标保持稳定.
  - 结论: 当前 `tns_max_bands` 截断口径更优, 不应放宽.
- 试探改动与结果:
  - `crates/tao-codec/src/decoders/aac/spectral.rs`:
    - 临时加入 long band_limit 探针.
    - 轻微退化后已完整回滚.
- P0 指标变化:
  - 回滚后恢复:
    - `data/1.m4a`: `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0`.
    - `data/2.m4a`: `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`, `lag=0`.
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(回滚), 但收敛 TNS 频带边界假设`.
- 下一子功能: `4-2` 继续(转向 short/long overlap 状态衔接的对齐诊断).

## Round-37 (4-2: LONG_START 尾段窗形状探针)

- 子功能: `4-2`.
- 对比结论: `不一致(退化)`.
- 逻辑证据:
  - 试探: `LONG_START` 尾段短窗后半由 `curr_shape` 临时改为 `prev_shape`.
  - 回归结果:
    - `data/1.m4a`: `精度 99.883833% -> 99.872255%`, `psnr 38.87dB -> 38.45dB`.
    - `data/2.m4a`: 指标保持稳定.
  - 结论: LONG_START 尾段应继续使用 `curr_shape`, 当前实现更优.
- 试探改动与结果:
  - `crates/tao-codec/src/decoders/aac/imdct.rs`:
    - 临时加入 `LONG_START` 尾段 shape 切换探针.
    - 退化后已完整回滚.
- P0 指标变化:
  - 回滚后恢复:
    - `data/1.m4a`: `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0`.
    - `data/2.m4a`: `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`, `lag=0`.
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(回滚), 但收敛 LONG_START 窗形状假设`.
- 下一子功能: `4-2` 继续(转向 LONG_STOP 前段窗形状探针).

## Round-38 (4-2: LONG_STOP 前段窗形状探针)

- 子功能: `4-2`.
- 对比结论: `不一致(退化)`.
- 逻辑证据:
  - 试探: `LONG_STOP` 前段短窗由 `short_prev` 临时切到 `short_curr`.
  - 回归结果:
    - `data/1.m4a`: `max_err=0.478541881`, `psnr=37.47dB`, `精度=99.839969%`.
    - `data/2.m4a`: 指标保持稳定.
  - 结论: LONG_STOP 前段应保持 `short_prev` 口径.
- 试探改动与结果:
  - `crates/tao-codec/src/decoders/aac/imdct.rs`:
    - 临时加入 LONG_STOP shape 切换探针.
    - 退化后已完整回滚.
- P0 指标变化:
  - 回滚后恢复:
    - `data/1.m4a`: `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0`.
    - `data/2.m4a`: `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`, `lag=0`.
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(回滚), 但收敛 LONG_STOP 窗形状假设`.
- 下一子功能: `4-2` 继续(转向 LONG_STOP 前段索引方向探针).

## Round-39 (4-2: LONG_STOP 前段索引方向探针)

- 子功能: `4-2`.
- 对比结论: `不一致(强退化)`.
- 逻辑证据:
  - 试探: `LONG_STOP` 前段短窗索引临时改为反向(`127-k`).
  - 回归结果:
    - `data/1.m4a`: `max_err=1.116684735`, `psnr=28.65dB`, `精度=98.791502%`.
    - `data/2.m4a`: `max_err=0.867259540`, `psnr=32.67dB`, `精度=99.482866%`.
  - 结论: LONG_STOP 前段索引方向当前实现(正向)正确.
- 试探改动与结果:
  - `crates/tao-codec/src/decoders/aac/imdct.rs`:
    - 临时加入 LONG_STOP 前段反向索引探针.
    - 强退化后已完整回滚.
- P0 指标变化:
  - 回滚后恢复:
    - `data/1.m4a`: 回到 `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0`.
    - `data/2.m4a`: 回到 `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`, `lag=0`.
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(回滚), 但收敛 LONG_STOP 索引方向假设`.
- 下一子功能: `4-2` 继续(转向 short/long 过渡区 overlap 状态语义差分诊断).

## Round-40 (4-2: common_window 双噪声频带 PNS 耦合探针)

- 子功能: `4-2`.
- 对比结论: `不一致(无收益)`.
- 逻辑证据:
  - 试探: 在 `common_window` 下, 当左右声道同频带均为 `NOISE_HCB` 时, 增加临时 PNS 耦合复制(`right = +/- left`).
  - 回归结果:
    - `data/1.m4a`: 指标保持 `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`.
    - `data/2.m4a`: 指标保持 `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`.
  - 结论: 当前主残差不由该 PNS 耦合缺口驱动.
- 试探改动与结果:
  - `crates/tao-codec/src/decoders/aac/mod.rs`:
    - 临时加入 `TAO_AAC_PNS_COUPLE` 调用开关.
  - `crates/tao-codec/src/decoders/aac/spectral.rs`:
    - 临时新增 `apply_pns_coupling(...)`.
  - 无收益后上述改动已完整回滚.
- P0 指标变化:
  - 回滚后保持:
    - `data/1.m4a`: `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0`.
    - `data/2.m4a`: `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`, `lag=0`.
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(回滚), 但排除 common_window 双噪声频带 PNS 耦合假设`.
- 下一子功能: `4-2` 继续(转向频谱 tuple 写入边界语义与参考实现对齐探针).

## Round-41 (4-2: 频谱 tuple 写入边界语义探针)

- 子功能: `4-2`.
- 对比结论: `不一致(无收益)`.
- 逻辑证据:
  - 试探: 在频谱解码时, 去掉 `count=min(dim, band_remain)` 截断, 临时改为按码本维度完整写入(tuple 全维写入), 以对齐 FFmpeg tuple 写入语义.
  - 回归结果:
    - `data/1.m4a`: 指标保持 `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`.
    - `data/2.m4a`: 指标保持 `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`.
  - 结论: 当前主残差不由 tuple 边界写入语义驱动.
- 试探改动与结果:
  - `crates/tao-codec/src/decoders/aac/spectral.rs`:
    - 临时移除 short/long 解码中的 `count=min(...)` 截断.
  - 无收益后已完整回滚至原实现.
- P0 指标变化:
  - 回滚后保持:
    - `data/1.m4a`: `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0`.
    - `data/2.m4a`: `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`, `lag=0`.
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(回滚), 但完成 tuple 写入语义假设排除`.
- 下一子功能: `4-2` 继续(转向 KBD/sine 窗构建与参考实现的数值一致性诊断).

## Round-42 (4-2: KBD 窗构建数值精度探针)

- 子功能: `4-2`.
- 对比结论: `不一致(无收益)`.
- 逻辑证据:
  - 试探: 提高 `bessel_i0` 级数截断精度(`1e-12 -> 1e-15`, `k 上限 50 -> 200`), 以降低 KBD 窗构建累计误差.
  - 回归结果:
    - `data/1.m4a`: 指标保持 `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`.
    - `data/2.m4a`: 指标保持 `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`.
  - 结论: 当前 P0 主残差不由 KBD 窗级数截断精度驱动.
- 试探改动与结果:
  - `crates/tao-codec/src/decoders/aac/imdct.rs`:
    - 临时提高 `bessel_i0` 收敛阈值与迭代上限.
  - 无收益后已完整回滚.
- P0 指标变化:
  - 回滚后保持:
    - `data/1.m4a`: `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0`.
    - `data/2.m4a`: `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`, `lag=0`.
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(回滚), 但完成 KBD 级数精度假设排除`.
- 下一子功能: `4-2` 继续(转向 short path 的 group 基址与 swb 映射一致性探针).

## Round-43 (4-2: short scale_factor_grouping 位序反向探针)

- 子功能: `4-2`.
- 对比结论: `不一致(强退化)`.
- 逻辑证据:
  - 试探: 将 short `scale_factor_grouping` 解析位序由 `(6-i)` 临时改为 `(i)`.
  - 回归结果(`data/1.m4a`):
    - `max_err=2705606.637483791`, `psnr=-90.66dB`, `corr=-0.000204170`, 直接触发“最大误差超阈值且 PSNR 过低”.
  - 结论: 当前实现的 short grouping 位序 `(6-i)` 为正确口径, 反向位序错误.
- 试探改动与结果:
  - `crates/tao-codec/src/decoders/aac/mod.rs`:
    - 临时修改 `parse_ics_info` short grouping 位序.
  - 强退化后已完整回滚.
- P0 指标变化:
  - 回滚后恢复:
    - `data/1.m4a`: `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0`.
    - `data/2.m4a`: 基线保持 `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`, `lag=0`.
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(回滚), 但完成 short grouping 位序假设排除`.
- 下一子功能: `4-2` 继续(转向 short/long 过渡段的窗表取值边界探针).

## Round-44 (4-2: LONG_START 尾段短窗索引边界 `-1` 探针)

- 子功能: `4-2`.
- 对比结论: `不一致(轻微退化)`.
- 逻辑证据:
  - 试探: `LONG_START` 尾段短窗索引由 `128 + (i-1472)` 临时改为 `127 + (i-1472)`.
  - 回归结果(`data/1.m4a`):
    - `精度 99.883833% -> 99.883772%`, `psnr 38.87dB -> 38.86dB`, `max_err` 不变.
  - 结论: 当前 `LONG_START` 尾段起始索引 `128` 更优.
- 试探改动与结果:
  - `crates/tao-codec/src/decoders/aac/imdct.rs`:
    - 临时调整 LONG_START 尾段短窗索引起点.
  - 轻微退化后已完整回滚.
- P0 指标变化:
  - 回滚后恢复:
    - `data/1.m4a`: `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0`.
    - `data/2.m4a`: 保持 `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`, `lag=0`.
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(回滚), 但收敛 LONG_START 尾段索引边界假设`.
- 下一子功能: `4-2` 继续(转向 LONG_STOP 前段短窗索引边界 `+1` 探针).

## Round-45 (4-2: LONG_STOP 前段短窗索引边界 `+1` 探针)

- 子功能: `4-2`.
- 对比结论: `不一致(退化)`.
- 逻辑证据:
  - 试探: `LONG_STOP` 前段短窗索引由 `i-448` 临时改为 `i-447`(上限钳制).
  - 回归结果(`data/1.m4a`):
    - `精度 99.883833% -> 99.883675%`, `psnr 38.87dB -> 38.86dB`, `corr 0.999418330 -> 0.999417530`.
  - 结论: 当前 `LONG_STOP` 前段索引起点 `i-448` 更优.
- 试探改动与结果:
  - `crates/tao-codec/src/decoders/aac/imdct.rs`:
    - 临时调整 LONG_STOP 前段短窗索引起点.
  - 退化后已完整回滚.
- P0 指标变化:
  - 回滚后恢复:
    - `data/1.m4a`: `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0`.
    - `data/2.m4a`: 保持 `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`, `lag=0`.
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(回滚), 但收敛 LONG_STOP 前段索引边界假设`.
- 下一子功能: `4-2` 继续(转向 LONG_START/LONG_STOP 平顶区边界长度探针).

## Round-46 (4-2: LONG_START 平顶区长度 `+1` 探针)

- 子功能: `4-2`.
- 对比结论: `不一致(轻微退化)`.
- 逻辑证据:
  - 试探: `LONG_START` 平顶区右边界由 `<1472` 临时改为 `<1473`, 同时尾段短窗区间整体后移 1 点.
  - 回归结果(`data/1.m4a`):
    - `精度 99.883833% -> 99.883773%`, `psnr 38.87dB -> 38.86dB`.
  - 结论: 当前 LONG_START 平顶区长度与尾段边界口径更优, 不应后移.
- 试探改动与结果:
  - `crates/tao-codec/src/decoders/aac/imdct.rs`:
    - 临时调整 LONG_START 平顶区与尾段边界.
  - 轻微退化后已完整回滚.
- P0 指标变化:
  - 回滚后恢复:
    - `data/1.m4a`: `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0`.
    - `data/2.m4a`: 保持 `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`, `lag=0`.
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(回滚), 但收敛 LONG_START 平顶区边界假设`.
- 下一子功能: `4-2` 继续(转向 LONG_STOP 平顶区边界长度探针).

## Round-47 (4-2: LONG_STOP 平顶区起点前移 1 点探针)

- 子功能: `4-2`.
- 对比结论: `无差异`.
- 逻辑证据:
  - 试探: 将 `LONG_STOP` 前段边界由 `<576` 临时改为 `<575`, 即平顶区起点前移 1 点.
  - 回归结果(`data/1.m4a`): 与基线完全一致:
    - `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `corr=0.999418330`.
  - 结论: 该单点边界前移对当前 P0 指标无可观影响.
- 试探改动与结果:
  - `crates/tao-codec/src/decoders/aac/imdct.rs`:
    - 临时调整 LONG_STOP 前段/平顶区边界.
  - 无差异后已回滚以保持主线最小改动.
- P0 指标变化:
  - `data/1.m4a`: 保持 `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0`.
  - `data/2.m4a`: 基线保持 `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`, `lag=0`.
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(回滚), 但完成 LONG_STOP 平顶区起点单点边界排除`.
- 下一子功能: `4-2` 继续(转向 LONG_STOP 平顶区终点/long 窗接入边界探针).

## Round-48 (4-2: LONG_STOP 平顶区终点 `1024->1023` 探针)

- 子功能: `4-2`.
- 对比结论: `无差异`.
- 逻辑证据:
  - 试探: 将 `LONG_STOP` 平顶区终点由 `<1024` 临时改为 `<1023`, 即 long 窗接入提前 1 点.
  - 回归结果(`data/1.m4a`)与基线完全一致:
    - `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `corr=0.999418330`.
  - 结论: 该单点边界调整不是当前主残差来源.
- 试探改动与结果:
  - `crates/tao-codec/src/decoders/aac/imdct.rs`:
    - 临时调整 LONG_STOP 平顶区终点.
  - 无差异后已回滚.
- P0 指标变化:
  - `data/1.m4a`: 保持 `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0`.
  - `data/2.m4a`: 基线保持 `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`, `lag=0`.
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(回滚), 但完成 LONG_STOP 平顶区终点单点边界排除`.
- 下一子功能: `4-2` 继续(转向窗口切换边界的多点联合探针).

## Round-49 (4-2: LONG_START/LONG_STOP 索引偏移网格扫描)

- 子功能: `4-2`.
- 对比结论: `无差异(基线最优)`.
- 逻辑证据:
  - 新增临时环境变量探针:
    - `TAO_AAC_LS_INDEX_SHIFT`
    - `TAO_AAC_LSTOP_INDEX_SHIFT`
  - 扫描范围: `{-1,0,1} x {-1,0,1}` 共 9 组.
  - 扫描结果显示 `ls_index=0, lstop_index=0` 为最优:
    - `precision=99.883833%`, `psnr=38.87dB`, `max_err=0.476204097`.
  - 其余 8 组均轻微退化.
- 试探改动与结果:
  - `crates/tao-codec/src/decoders/aac/imdct.rs`:
    - 临时加入窗口边界/索引偏移环境变量读取逻辑(默认 0 行为不变), 用于批量扫描.
  - 当前保留该探针框架以加速后续参数排查.
- P0 指标变化:
  - 默认参数(不设环境变量)保持:
    - `data/1.m4a`: `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0`.
    - `data/2.m4a`: `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`, `lag=0`.
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(无精度提升), 但形成可复用的参数扫描基建`.
- 下一子功能: `4-2` 继续(转向边界偏移参数网格扫描).

## Round-50 (4-2: 边界偏移参数 2D 网格扫描)

- 子功能: `4-2`.
- 对比结论: `无差异(基线最优或并列最优)`.
- 逻辑证据:
  - 扫描参数:
    - `TAO_AAC_LS_BOUNDARY_SHIFT ∈ {-1,0,1}`
    - `TAO_AAC_LSTOP_BOUNDARY_SHIFT ∈ {-1,0,1}`
  - 扫描结果:
    - 当 `LS_BOUNDARY_SHIFT=0` 时, 精度保持 `99.883833%`（与基线并列最优）.
    - `LS_BOUNDARY_SHIFT=-1`/`+1` 均出现轻微退化到 `99.883782%`/`99.883773%`.
    - `LSTOP_BOUNDARY_SHIFT` 在该范围内对结果几乎无影响.
  - 结论: 局部边界偏移无法带来有效提升.
- 试探改动与结果:
  - 沿用 `imdct.rs` 中环境变量探针框架进行批量扫描, 默认行为不变.
- P0 指标变化:
  - 默认参数下保持:
    - `data/1.m4a`: `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0`.
    - `data/2.m4a`: `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`, `lag=0`.
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(无提升), 但边界参数空间进一步收敛`.
- 下一子功能: `4-2` 继续(转向 4 维联合扫描与候选复核).

## Round-51 (4-2: 4 维联合扫描与候选复核)

- 子功能: `4-2`.
- 对比结论: `不一致(候选被否决)`.
- 逻辑证据:
  - 扫描参数:
    - `LS_BOUNDARY_SHIFT`, `LS_INDEX_SHIFT`, `LSTOP_BOUNDARY_SHIFT`, `LSTOP_INDEX_SHIFT`
    - 各取 `{-1,0,1}`, 共 `81` 组.
  - `data/1.m4a` 最优候选:
    - `LS_BOUNDARY_SHIFT=1`, `LS_INDEX_SHIFT=1`, `LSTOP_INDEX_SHIFT=0` (`LSTOP_BOUNDARY_SHIFT` 任意),
    - 精度 `99.883834%`（较基线仅 +0.000001%）.
  - 候选在 `data/2.m4a` 复核回退:
    - `max_err 0.000196695 -> 0.003806889`
    - `psnr 86.95dB -> 86.42dB`
  - 结论: 候选不满足 P0 双样本一致提升要求, 不可采纳.
- 试探改动与结果:
  - 继续使用 `imdct.rs` 环境变量探针完成 81 组批量扫描和候选验证.
  - 采纳策略: 仍以默认 `0/0/0/0` 作为运行口径.
- P0 指标变化:
  - 最终默认参数保持:
    - `data/1.m4a`: `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0`.
    - `data/2.m4a`: `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`, `lag=0`.
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(候选回退), 但完成窗口参数局部最优搜索`.
- 下一子功能: `4-2` 继续(转向代码路径级差分, 非参数扫描方向).

## Round-52 (4-2: `packet.duration` 输出裁剪路径探针)

- 子功能: `4-2`.
- 对比结论: `无差异`.
- 逻辑证据:
  - 试探: 临时增加 `TAO_AAC_FORCE_FRAME1024=1`, 强制每包输出 `1024` 样本, 排查 `packet.duration` 裁剪路径对时域边界的影响.
  - 回归结果:
    - `data/1.m4a`: 与基线完全一致 (`99.883833%`, `psnr=38.87dB`, `max_err=0.476204097`).
    - `data/2.m4a`: 与基线完全一致 (`99.999998%`, `psnr=86.95dB`, `max_err=0.000196695`).
  - 结论: 当前主残差不由 `packet.duration` 输出裁剪口径驱动.
- 试探改动与结果:
  - `crates/tao-codec/src/decoders/aac/mod.rs`:
    - 临时加入 `TAO_AAC_FORCE_FRAME1024` 分支.
  - 无差异后已完整回滚.
- P0 指标变化:
  - 回滚后保持:
    - `data/1.m4a`: `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0`.
    - `data/2.m4a`: `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`, `lag=0`.
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(回滚), 但完成输出裁剪路径假设排除`.
- 下一子功能: `4-2` 继续(转向 short 合成与 overlap 状态机的结构化差分).

## Round-53 (4-2: PNS 目标幅值符号口径探针)

- 子功能: `4-2`.
- 对比结论: `无差异(轻微退化)`.
- 逻辑证据:
  - FDK 参考: `libAACdec/src/block.cpp` 中 `ApplyTools -> CPns_Apply` 路径未给出可证明的“统一反相可提升”证据.
  - FFmpeg 参考: `libavcodec/aac/aacdec_proc_template.c` 中 NOISE_BT 路径 `scale = sf[idx] / sqrt(band_energy)`, 其中 `sf[idx]` 在 `dequant_scalefactors` 的 NOISE_BT 分支为负值.
  - 探针假设: Tao 的 PNS 目标幅值若改为负号, 可能更贴近 FFmpeg 路径并降低多块离散误差.
  - 实测结果:
    - `data/1.m4a`: `max_err 0.476204097 -> 0.476205051` (微退化), `psnr/precision` 保持 `38.87dB/99.883833%`.
    - `data/2.m4a`: 与基线一致 (`max_err=0.000196695`, `psnr=86.95dB`, `precision=99.999998%`).
  - 结论: PNS 目标符号不是当前 P0 主残差根因.
- 试探改动与结果:
  - `crates/tao-codec/src/decoders/aac/spectral.rs`:
    - 临时把 `NOISE_HCB` 的 `target` 改为负号.
  - P0 无提升且 `data/1` 出现微退化后已回滚.
- P0 指标变化:
  - 回滚后保持:
    - `data/1.m4a`: `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0`.
    - `data/2.m4a`: `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`, `lag=0`.
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(回滚), 但完成 PNS 目标符号假设排除`.
- 下一子功能: `4-2` 继续(转向 short/long overlap 状态机与 FFmpeg `imdct_and_windowing` 的实现级对齐).

## Round-54 (4-2: short 首窗左半窗形状口径探针)

- 子功能: `4-2`.
- 对比结论: `无差异`.
- 逻辑证据:
  - FDK/FFmpeg 口径均依赖 `window_shape` 历史状态参与 short 首窗左半窗过渡.
  - 探针假设: Tao 若错误使用 `prev_window_shape`, 则“强制首窗左半统一使用 `curr_window_shape`”应改变 P0 指标.
  - 实测结果:
    - `data/1.m4a`: 指标完全不变 (`max_err=0.476204097`, `psnr=38.87dB`, `precision=99.883833%`).
    - `data/2.m4a`: 指标完全不变 (`max_err=0.000196695`, `psnr=86.95dB`, `precision=99.999998%`).
  - 结论: 首窗左半窗形状选择不是当前主残差来源.
- 试探改动与结果:
  - `crates/tao-codec/src/decoders/aac/imdct.rs`:
    - 临时把 `win=0` 的 `short_left` 从 `short_prev` 改为 `short_curr`.
  - 无差异后已回滚.
- P0 指标变化:
  - 回滚后保持:
    - `data/1.m4a`: `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0`.
    - `data/2.m4a`: `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`, `lag=0`.
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(回滚), 但完成 short 首窗左半窗形状假设排除`.
- 下一子功能: `4-2` 继续(转向 FFmpeg `imdct_and_windowing` 输出/保存状态机逐段对齐探针).

## Round-55 (4-2: FFmpeg overlap 状态机分段混合探针)

- 子功能: `4-2`.
- 对比结论: `不一致(显著退化)`.
- 逻辑证据:
  - FFmpeg 参考: `libavcodec/aac/aacdec_dsp_template.c::imdct_and_windowing` 在非 long-long 组合下使用分段混合(`out[0..448]` 直接取 `saved`, 后续按 short/long 分支 overlap).
  - 探针假设: Tao 若在 overlap-add 状态机上存在结构性偏差, 则按 FFmpeg 分段规则近似改写应提升 P0.
  - 探针实现:
    - 在 `decode_raw_frame` 增加临时环境变量 `TAO_AAC_FFMPEG_OLA_PROBE=1`.
    - 在 probe 模式下启用分段混合与 `LONG_START` 特化保存策略.
  - 实测结果(开启 probe):
    - `data/1.m4a`: `99.883833% -> 99.798113%`, `psnr 38.87dB -> 36.46dB`, `max_err 0.476204097 -> 0.856235653`.
    - `data/2.m4a`: `99.999998% -> 99.833496%`, `psnr 86.95dB -> 37.60dB`, `max_err 0.000196695 -> 0.707782710`.
  - 结论: 该“分段近似”与当前 IMDCT/窗实现不兼容, 会显著破坏双样本精度.
- 试探改动与结果:
  - `crates/tao-codec/src/decoders/aac/mod.rs`:
    - 临时加入 `TAO_AAC_FFMPEG_OLA_PROBE` 分支及相关状态字段.
  - 结果显著退化后已完整回滚.
- P0 指标变化:
  - 回滚后恢复基线:
    - `data/1.m4a`: `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0`.
    - `data/2.m4a`: `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`, `lag=0`.
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(回滚), 但确认不能在现有 IMDCT 输出语义上直接套用 FFmpeg 分段 overlap 近似`.
- 下一子功能: `4-2` 继续(转向“IMDCT 输出语义与 FFmpeg/FDK 保存状态同构化”前置探针).

## Round-56 (4-2: PNS 幅值口径与热点块特征复核)

- 子功能: `4-2`.
- 对比结论: `不一致(探针显著退化)`.
- 逻辑证据:
  - FFmpeg 参考:
    - `libavcodec/aac/aacdec.c::TYPE_FIL` 与 `libavcodec/aac/aacdec.c::skip_data_stream_element` 的 `FIL/DSE` 跳过语义确认完成.
    - `libavcodec/aac/aacdec.c::decode_scalefactors` + `libavcodec/aac/aacdec_dsp_template.c::decode_spectrum_and_dequant` 复核 PNS 幅值链路.
  - FDK 参考:
    - `libAACdec/src/aacdecoder.cpp::ID_FIL` 的 `bitCnt = esc_count + 14` 与 Tao 当前 `count += esc_count - 1` 语义等价.
    - `libAACdec/src/channel.cpp::MapMidSideMaskToPnsCorrelation` 与 `libAACdec/src/aacdec_pns.cpp::CPns_Apply` 复核到“PNS 相关性”路径存在专门处理.
  - 诊断结果:
    - 对 `data/1.m4a` 高误差块(`raw_block=11/104/132/239/320/361`)做块级语法复核, 均命中 `CPE common_window=1`, 且均存在 `PNS`.
    - 其中主热点 `raw_block=320` 特征为 `win_seq=2(short), win_shape=0, pns_l=1, pns_r=1, tns_l=true, tns_r=true`.
  - 探针假设:
    - 将 PNS 目标幅值由 `2^((sf-100)/4)` 改为 `2^(sf/4)` 可能贴近参考实现.
  - 实测结果:
    - `data/1.m4a` 显著退化: `99.883833% -> 47.229395%`, `max_err 0.476204097 -> 14.764705241`, `psnr 38.87dB -> 9.04dB`.
    - `data/2.m4a` 基本不变: `99.999998%`, `max_err=0.000196695`.
  - 结论:
    - 当前 Tao 的 PNS 幅值基线口径不能改为 `2^(sf/4)`.
    - `FIL/DSE` 跳过语义与 FDK/FFmpeg 核对一致, 不是本轮主残差来源.
- 试探改动与结果:
  - `crates/tao-codec/src/decoders/aac/spectral.rs`:
    - 临时修改 PNS 目标幅值口径; 出现强退化后已完整回滚.
  - `crates/tao-codec/src/decoders/aac/mod.rs` + `spectral.rs`:
    - 临时加入块级/clamp 诊断输出; 采样证据后已完整回滚.
- P0 指标变化:
  - 回滚后恢复基线:
    - `data/1.m4a`: `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0`.
    - `data/2.m4a`: `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`, `lag=0`.
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(回滚), 但完成 PNS 幅值主假设否定与热点块特征收敛`.
- 下一子功能: `3-3/4-2` 联动(转向 PNS 立体声相关性与随机种子顺序口径对齐探针, 重点复核 `common_window + PNS` 组合).

## Round-57 (3-3/4-2: PNS 立体声相关性探针)

- 子功能: `3-3/4-2`.
- 对比结论: `无差异`.
- 逻辑证据:
  - FDK 参考:
    - `libAACdec/src/channel.cpp::MapMidSideMaskToPnsCorrelation`
    - `libAACdec/src/aacdec_pns.cpp::CPns_Apply`
    - FDK 在 `common_window + MsUsed + 双声道PNS` 组合上存在相关性路径.
  - 探针假设:
    - Tao 在该组合下缺失“相关噪声复用”会引入短块误差; 通过“右声道跟随左声道并按 sf 比例缩放”可改善 P0.
  - 探针实现:
    - `spectral.rs` 临时增加 `apply_pns_stereo_correlation_probe(...)`.
    - `mod.rs` 在 common-window 的 `MS` 后调用该探针.
    - 仅在 `TAO_AAC_PNS_CORR_PROBE=1` 开启.
  - 实测结果(开启 probe):
    - `data/1.m4a`: 指标完全不变 (`max_err=0.476204097`, `psnr=38.87dB`, `precision=99.883833%`).
    - `data/2.m4a`: 指标完全不变 (`max_err=0.000196695`, `psnr=86.95dB`, `precision=99.999998%`).
  - 结论:
    - 本轮“相关性探针”未产生可观影响, 不是当前 P0 残差的直接决定因素.
- 试探改动与结果:
  - `crates/tao-codec/src/decoders/aac/spectral.rs` + `mod.rs`:
    - 临时加入 `TAO_AAC_PNS_CORR_PROBE` 分支.
    - 无差异后已完整回滚.
- P0 指标变化:
  - 回滚后保持:
    - `data/1.m4a`: `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0`.
    - `data/2.m4a`: `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`, `lag=0`.
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(回滚), 但完成 PNS 相关性路径假设排除`.
- 下一子功能: `4-2` 继续(转向 short 块内 TNS 作用区间与 PNS 频带重叠位置的组合探针).

## Round-58 (4-2: TNS `map_idx` 组合口径探针)

- 子功能: `4-2`.
- 对比结论: `不一致(探针显著退化)`.
- 逻辑证据:
  - FFmpeg 参考:
    - `libavcodec/aac/aacdec.c::ff_aac_decode_tns` 明确使用 `tmp2_idx = 2 * coef_compress + coef_res`.
  - FDK 参考:
    - `libAACdec/src/block.cpp::ApplyTools` 显示 AAC-LC 工具链顺序为 `PNS -> TNS`.
    - `libAACdec/src/aacdec_tns.cpp::CTns_Read`/`CTns_Apply` 的滤波器字段读写与当前 Tao 主线一致.
  - 探针假设:
    - 若 Tao 在 `coef_res/coef_compress` 组合索引上存在口径错误, 切换到 `2 * coef_res + coef_compress` 可能改善 `data/1.m4a` 的 short+TNS 热点误差.
  - 实测结果(开启 `TAO_AAC_TNS_MAP_MODE=alt`):
    - `data/1.m4a`: 指标完全不变 (`max_err=0.476204097`, `psnr=38.87dB`, `precision=99.883833%`).
    - `data/2.m4a`: 明显退化到 `precision=96.374712%`, `psnr=24.07dB`, `max_err=1.490220845`.
  - 结论:
    - 当前主线 `map_idx = 2 * coef_compress + coef_res` 口径正确.
    - `map_idx` 组合方式不是 `data/1.m4a` 当前残差主因.
- 试探改动与结果:
  - `crates/tao-codec/src/decoders/aac/spectral.rs`:
    - 临时加入 `TAO_AAC_TNS_MAP_MODE=alt` 探针分支.
    - 验证后已完整回滚.
- P0 指标变化:
  - 回滚后恢复并复核:
    - `data/1.m4a`: `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0`.
    - `data/2.m4a`: `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`, `lag=0`.
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(回滚), 但完成 TNS map_idx 组合口径排除`.
- 下一子功能: `4-2` 继续(转向 short 路径 IMDCT/save 状态机与 FFmpeg 同构实现, 不再做纯参数探针).

## Round-59 (4-2: 非 long-long 路径 `saved` 参与区间探针)

- 子功能: `4-2`.
- 对比结论: `不一致(探针显著退化)`.
- 逻辑证据:
  - FFmpeg 参考:
    - `libavcodec/aac/aacdec_dsp_template.c::imdct_and_windowing` 在非 long-long 路径中, `saved` 只参与前段拼接, 后段主要由当前 `buf` 构造.
  - 探针假设:
    - Tao 当前统一 `overlap[0..1024] + windowed[0..1024]` 可能过度引入 `saved` 后段影响; 若仅在过渡路径限制 `saved` 参与前半段, 可能降低 short 热点误差.
  - 探针实现:
    - 在 `decode_raw_frame` 增加临时开关 `TAO_AAC_FFMPEG_SAVED_LIMIT_PROBE=1`.
    - 开启后, 在非 long-long 路径中仅 `i < 512` 使用 `overlap + windowed`, `i >= 512` 仅使用 `windowed`.
  - 实测结果(开启 probe):
    - `data/1.m4a`: 明显退化到 `precision=99.081032%`, `psnr=29.85dB`, `max_err=1.394778392`.
    - `data/2.m4a`: 明显退化到 `precision=99.382893%`, `psnr=31.89dB`, `max_err=0.816912080`.
  - 结论:
    - 当前 Tao 不能通过“简单截断 saved 参与区间”逼近 FFmpeg.
    - 该问题需要结构化同构实现, 不能用单点区间裁剪近似.
- 试探改动与结果:
  - `crates/tao-codec/src/decoders/aac/mod.rs`:
    - 临时加入 `TAO_AAC_FFMPEG_SAVED_LIMIT_PROBE` 分支及 `prev_window_sequence` 状态.
    - 强退化后已完整回滚.
- P0 指标变化:
  - 回滚后恢复并复核:
    - `data/1.m4a`: `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0`.
    - `data/2.m4a`: `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`, `lag=0`.
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(回滚), 但完成“saved 区间裁剪近似”假设排除`.
- 下一子功能: `4-2` 继续(转向 short 路径 `saved/out` 状态机的结构化同构实现).

## Round-60 (4-2: TNS “有符号索引映射”探针)

- 子功能: `4-2`.
- 对比结论: `不一致(探针显著退化)`.
- 逻辑证据:
  - FDK 参考:
    - `libAACdec/src/aacdec_tns.cpp::CTns_Read` 对 TNS 系数采用符号扩展语义.
    - `libAACdec/src/aac_rom.cpp::FDKaacDec_tnsCoeff3/4` 表为“负到正”有序.
  - 探针假设:
    - Tao 当前按 FFmpeg 表直接索引, 若与“有符号索引语义”存在解释偏差, 可能导致 `data/1.m4a` short+TNS 热点误差.
  - 探针实现:
    - 在 `tns_coef_from_index` 增加临时开关 `TAO_AAC_TNS_SIGNED_MAP_PROBE=1`.
    - 开启后对 `q` 做“半区旋转+符号翻转”重排, 近似模拟有符号索引映射.
  - 实测结果(开启 probe):
    - `data/1.m4a`: 退化到 `precision=98.904925%`, `psnr=29.08dB`, `max_err=4.624514211`.
    - `data/2.m4a`: 严重退化到 `precision=81.342046%`, `psnr=16.22dB`, `max_err=67.992008820`.
  - 结论:
    - 当前 Tao 的 FFmpeg 风格 TNS 映射口径不可替换为该“有符号重排”近似.
    - `data/1.m4a` 残差并非由该映射语义驱动.
- 试探改动与结果:
  - `crates/tao-codec/src/decoders/aac/spectral.rs`:
    - 临时加入 `TAO_AAC_TNS_SIGNED_MAP_PROBE` 分支.
    - 强退化后已完整回滚.
- P0 指标变化:
  - 回滚后恢复并复核:
    - `data/1.m4a`: `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0`.
    - `data/2.m4a`: `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`, `lag=0`.
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(回滚), 但完成 TNS 有符号映射假设排除`.
- 下一子功能: `4-2` 继续(转向 short 路径 `saved/out` 状态机的实现级同构改造, 避免继续做表值口径探针).

## Round-61 (0-1/0-2 + 1-1~1-4: 探针清理 + FFT IMDCT 主路径落地)

- 子功能: `0-1`, `0-2`, `1-1`, `1-2`, `1-3`, `1-4`.
- 对比结论: `一致(结构化重写完成, P0 指标持平)`.
- 逻辑证据:
  - 功能点 0-1:
    - 清理残留环境变量探针:
      - `TAO_AAC_LS_BOUNDARY_SHIFT`
      - `TAO_AAC_LS_INDEX_SHIFT`
      - `TAO_AAC_LSTOP_BOUNDARY_SHIFT`
      - `TAO_AAC_LSTOP_INDEX_SHIFT`
      - `TAO_AAC_TNS_REVERSE_DIR_PROBE`
    - 移除 `read_window_shift` 路径, `apply_aac_long_window` 恢复固定常量边界.
  - 功能点 1-1/1-2/1-3:
    - 在 `imdct.rs` 新增自研复数 FFT 基础设施:
      - `Complex` 复数结构.
      - `fft_in_place` radix-2 蝶形实现.
    - 新增 `imdct_fft` 路径, 基于 `2N` 点复数 FFT 计算 DCT-IV 并映射到 IMDCT.
    - `imdct_1024` 与 `imdct_128` 均切换到 FFT 主路径(非 2 的幂输入保留 reference 回退).
  - 功能点 1-4:
    - 新增单测:
      - `test_imdct_fft_matches_reference_1024`
      - `test_imdct_fft_matches_reference_128`
    - 两个单测均通过, 误差阈值 `1e-5`.
- 修复改动:
  - `crates/tao-codec/src/decoders/aac/imdct.rs`:
    - 新增 `Complex` + `fft_in_place`.
    - 新增 `imdct_reference` 与 `imdct_fft`.
    - 删除窗口偏移探针代码, 恢复固定窗口边界路径.
    - 新增 IMDCT 数值一致性测试.
  - `crates/tao-codec/src/decoders/aac/spectral.rs`:
    - 删除 `TAO_AAC_TNS_REVERSE_DIR_PROBE` 分支.
- 验证命令与结果:
  - `cargo test -p tao-codec imdct_fft_matches_reference -- --nocapture` → `2 passed`.
  - `cargo test -p tao-codec decoders::aac -- --nocapture` → `20 passed`.
  - `TAO_AAC_COMPARE_INPUT=data/1.m4a cargo test --test run_decoder -- --nocapture --ignored test_aac_compare`:
    - 指标打印: `max_err=0.476204097`, `psnr=38.87dB`, `精度=99.883833%`, `lag=0`.
    - 结果仍未过 `99.9%` 门限(符合历史基线, 本轮主改动为结构化重写).
  - `TAO_AAC_COMPARE_INPUT=data/2.m4a cargo test --test run_decoder -- --nocapture --ignored test_aac_compare`:
    - `max_err=0.000196695`, `psnr=86.95dB`, `精度=99.999998%`, `lag=0`.
- P0 指标变化:
  - `data/1.m4a`: `99.883833% -> 99.883833%` (持平).
  - `data/2.m4a`: `99.999998% -> 99.999998%` (持平).
- P1 指标变化: `本轮未执行全量`.
- 判定: `有效修复(完成 v2 先决清理与 IMDCT 结构化重写, 为后续 overlap/window 同构改造提供稳定基线)`.
- 下一子功能: `2-1` (KBD 窗构建逐点对齐验证).

## Round-62 (2-1/2-2: 窗函数数值一致性基线固化)

- 子功能: `2-1`, `2-2`.
- 对比结论: `一致(窗函数数值口径固化, 指标保持稳定)`.
- 逻辑证据:
  - 新增 KBD 窗逐点对齐测试:
    - `test_kbd_window_matches_ffmpeg_reference_2048`
    - `test_kbd_window_matches_ffmpeg_reference_256`
  - 新增 sine 窗精度测试:
    - `test_sine_window_precision`
  - 新增 `bessel_i0` 锚点精度测试:
    - `test_bessel_i0_precision_anchor_points`
  - 执行 `cargo test -p tao-codec decoders::aac::imdct::tests -- --nocapture`:
    - 6 项全部通过.
  - 执行 `cargo test -p tao-codec decoders::aac -- --nocapture`:
    - 24 项全部通过.
- 修复改动:
  - `crates/tao-codec/src/decoders/aac/imdct.rs`:
    - 增加窗函数与贝塞尔函数数值一致性测试集.
- P0 指标变化:
  - `data/1.m4a`: 参考 Round-61 基线, 保持 `99.883833%`.
  - `data/2.m4a`: 参考 Round-61 基线, 保持 `99.999998%`.
- P1 指标变化: `本轮未执行全量`.
- 判定: `有效修复(完成功能点 2 的数值验证前置, 为 2-3/2-4 窗口路径逐行对齐提供稳定锚点)`.
- 下一子功能: `2-3` (LONG_START/LONG_STOP 过渡区窗口索引与 FFmpeg 逐行对比).

## Round-63 (2-3/2-4: 过渡窗口与 short 乘窗路径对齐测试固化)

- 子功能: `2-3`, `2-4`.
- 对比结论: `一致(窗口路径行为与参考实现一致)`.
- 逻辑证据:
  - 新增 `apply_aac_long_window` 参考对齐测试:
    - `test_apply_aac_long_window_matches_reference_long_start`
    - `test_apply_aac_long_window_matches_reference_long_stop`
  - 新增 short 合成/乘窗参考对齐测试:
    - `test_synthesize_short_windows_matches_reference`
  - 以上测试均采用独立参考实现与主线函数逐点比较.
  - 执行 `cargo test -p tao-codec decoders::aac::imdct::tests -- --nocapture`:
    - 9 项全部通过.
  - 执行 `cargo test -p tao-codec decoders::aac -- --nocapture`:
    - 27 项全部通过.
- 修复改动:
  - `crates/tao-codec/src/decoders/aac/imdct.rs`:
    - 增加 LONG_START/LONG_STOP 与 short 乘窗对齐测试.
- P0 指标变化:
  - 本轮未改动解码主路径逻辑, 维持 Round-61 基线:
    - `data/1.m4a`: `99.883833%`.
    - `data/2.m4a`: `99.999998%`.
- P1 指标变化: `本轮未执行全量`.
- 判定: `有效修复(完成功能点 2 全量验证, 可进入 overlap-add 状态机重写阶段)`.
- 下一子功能: `3-1` (ONLY_LONG overlap 与 saved 语义对比).

## Round-64 (3-1: overlap-add 状态机骨架重构)

- 子功能: `3-1`.
- 对比结论: `进行中(结构重构完成, 行为持平)`.
- 逻辑证据:
  - 在 `AacDecoder` 中新增 `prev_window_sequence` 声道状态.
  - 抽取 `overlap_add_with_sequence(...)`, 将 overlap-add 改为“按 `(prev_seq, curr_seq)` 显式分支”的状态机入口.
  - 当前分支实现保持与历史行为等价(输出仍为 `overlap + windowed[0..1024]`, `saved=windowed[1024..2048]`), 作为 3-2~3-5 的改写基座.
  - `cargo test -p tao-codec decoders::aac -- --nocapture` 全部通过(27/27).
  - P0 复测:
    - `data/1.m4a`: `99.883833%`, `max_err=0.476204097`, `lag=0`.
    - `data/2.m4a`: `99.999998%`, `max_err=0.000196695`, `lag=0`.
- 修复改动:
  - `crates/tao-codec/src/decoders/aac/mod.rs`:
    - 新增 `prev_window_sequence`.
    - 新增 `overlap_add_with_sequence`.
    - `decode_raw_frame` 改为通过状态机入口执行混合与 saved 更新.
  - `crates/tao-codec/src/decoders/aac/tests.rs`:
    - 同步 `AacDecoder` 测试构造字段.
- P0 指标变化:
  - `data/1.m4a`: 持平.
  - `data/2.m4a`: 持平.
- P1 指标变化: `本轮未执行全量`.
- 判定: `有效修复(完成状态机重写前置重构, 尚未引入 FFmpeg 分支语义差异)`.
- 下一子功能: `3-1` 继续(落地 ONLY_LONG 路径的 saved/out 语义同构改写).

## Round-65 (3-1/3-3/3-4: 分段输出规则落地, 指标持平)

- 子功能: `3-1`, `3-3`, `3-4`(分段输出骨架).
- 对比结论: `部分一致(结构更接近 FFmpeg, 指标持平)`.
- 逻辑证据:
  - `overlap_add_with_sequence` 改为显式三类分支:
    - long->long: 全段 overlap.
    - short: `[0..448]` 直接取 saved, `[448..1024]` overlap.
    - 非 short 过渡: `[0..448]` 取 saved, `[448..576]` overlap, `[576..1024]` 取当前块.
  - `cargo test -p tao-codec decoders::aac -- --nocapture` 全部通过.
  - P0 复测与基线完全一致:
    - `data/1.m4a`: `99.883833%`, `max_err=0.476204097`.
    - `data/2.m4a`: `99.999998%`, `max_err=0.000196695`.
- 修复改动:
  - `crates/tao-codec/src/decoders/aac/mod.rs`:
    - `overlap_add_with_sequence` 输出区间按窗口序列分支化.
- P0 指标变化:
  - 双样本持平.
- P1 指标变化: `本轮未执行全量`.
- 判定: `有效修复(结构前进, 无回归)`.
- 下一子功能: `3-2` (LONG_START saved 语义同构尝试).

## Round-66 (3-2: LONG_START saved 分段更新试探并回滚)

- 子功能: `3-2`.
- 对比结论: `不一致(实现试探显著退化)`.
- 逻辑证据:
  - 试探: `LONG_START` 的 saved 更新改为 `448 + 64 + 尾零` 分段形式.
  - 回归结果:
    - `data/1.m4a`: `99.883833% -> 99.798113%`, `max_err 0.476204097 -> 0.856235653`.
    - `data/2.m4a`: `99.999998% -> 99.833496%`, `max_err 0.000196695 -> 0.707782710`.
  - 判定该改动与当前“windowed+saved 表示法”不兼容.
- 修复改动:
  - `crates/tao-codec/src/decoders/aac/mod.rs`:
    - LONG_START saved 分段更新已完整回滚.
- P0 指标变化:
  - 回滚后恢复基线:
    - `data/1.m4a`: `99.883833%`, `max_err=0.476204097`.
    - `data/2.m4a`: `99.999998%`, `max_err=0.000196695`.
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(回滚), 但确认 3-2 需与 saved 数据表示同构改造联动推进`.
- 下一子功能: `3-2` 继续(先补状态机分支单测锚点).

## Round-67 (3-1: overlap 状态机分支单测锚点)

- 子功能: `3-1`.
- 对比结论: `一致(单测覆盖增强)`.
- 逻辑证据:
  - 新增 4 个状态机单测:
    - `test_overlap_add_with_sequence_first_frame_copy`
    - `test_overlap_add_with_sequence_long_to_long`
    - `test_overlap_add_with_sequence_short_branch_segments`
    - `test_overlap_add_with_sequence_long_stop_segments`
  - 执行:
    - `cargo test -p tao-codec decoders::aac::tests::test_overlap_add_with_sequence -- --nocapture` → `4 passed`.
    - `cargo test -p tao-codec decoders::aac -- --nocapture` → `31 passed`.
  - P0 复测保持基线不变.
- 修复改动:
  - `crates/tao-codec/src/decoders/aac/tests.rs`:
    - 新增 overlap-add 状态机分支行为测试.
- P0 指标变化:
  - `data/1.m4a`: `99.883833%` (持平).
  - `data/2.m4a`: `99.999998%` (持平).
- P1 指标变化: `本轮未执行全量`.
- 判定: `有效修复(状态机改造的回归锚点建立完成)`.
- 下一子功能: `3-2` 继续(探索 LONG_START/LONG_STOP saved 与输出语义同构重写, 避免“windowed 表示法”直接套用).

## Round-68 (4-1: 反量化数值路径优化)

- 子功能: `4-1`.
- 对比结论: `部分一致(实现优化, 指标基本持平)`.
- 逻辑证据:
  - 对 `inverse_quantize` 进行数值路径重构:
    - 增加 `pow43` 预计算缓存(0..8191).
    - 增加反量化 scale 预计算缓存(`sf=-256..511`).
    - 超范围回退到 `f64` 幂函数计算.
  - 新增单测 `test_inverse_quantize_matches_reference`, 对若干 `(x, sf)` 样本与参考公式逐点比对.
  - 执行:
    - `cargo test -p tao-codec decoders::aac::spectral::tests -- --nocapture` → `1 passed`.
    - `cargo test -p tao-codec decoders::aac -- --nocapture` → `32 passed`.
  - P0 复测:
    - `data/1.m4a`: `99.883833%`, `max_err=0.476204097` (持平).
    - `data/2.m4a`: `99.999998%`, `max_err=0.000196815` (与基线同量级, 微小浮点扰动).
- 修复改动:
  - `crates/tao-codec/src/decoders/aac/spectral.rs`:
    - `inverse_quantize` 改为表驱动+`f64` 回退.
    - 新增 `pow43_value`/`inverse_quant_scale`.
    - 新增 `spectral` 单测模块.
- P0 指标变化:
  - `data/1.m4a`: 持平.
  - `data/2.m4a`: 微扰级变化(无门限影响).
- P1 指标变化: `本轮未执行全量`.
- 判定: `有效修复(4-1 数值路径完成一轮优化, 后续继续联动 Huffman/反量化整体对齐)`.
- 下一子功能: `4-1` 继续(Huffman + 反量化链路逐行对齐).

## Round-69 (4-3/4-4: non-common CPE 强度立体声路径补齐)

- 子功能: `4-3`, `4-4`.
- 对比结论: `部分一致(路径补齐, 指标持平)`.
- 逻辑证据:
  - 对比 FFmpeg `apply_intensity_stereo` 调用位置后确认:
    - `CPE common_window=0` 仍可能需要执行 IS 重建.
    - 当前 Tao 在该分支仅解左右 ICS, 未执行 IS.
  - 本轮在 non-common CPE 分支中:
    - 解析右声道 ICS 时捕获 `band_type/scale_factors`.
    - 补充 `apply_intensity_stereo(..., ms_used=None)` 调用.
  - 同步做了两组探针并回滚:
    - short 首窗右半窗改用 `prev_shape` -> P0 完全不变.
    - Huffman 失败即报错探针 -> P0 完全不变, 说明当前样本不存在“静默截断”触发.
- 修复改动:
  - `crates/tao-codec/src/decoders/aac/mod.rs`:
    - non-common CPE 分支新增右声道 `IcsBandInfo` 捕获.
    - 新增 non-common 分支 `apply_intensity_stereo` 调用.
- 验证命令与结果:
  - `cargo test -p tao-codec decoders::aac -- --nocapture` -> `33 passed`.
  - `TAO_AAC_COMPARE_INPUT=data/1.m4a cargo test --test run_decoder -- --nocapture --ignored test_aac_compare`:
    - `99.883833%`, `max_err=0.476204097`, `lag=0` (持平).
  - `TAO_AAC_COMPARE_INPUT=data/2.m4a cargo test --test run_decoder -- --nocapture --ignored test_aac_compare`:
    - `99.999998%`, `max_err=0.000196815`, `lag=0` (持平).
- P0 指标变化:
  - `data/1.m4a`: 持平.
  - `data/2.m4a`: 持平.
- P1 指标变化: `本轮未执行全量`.
- 判定: `有效修复(语义补齐, 无回归)`.
- 下一子功能: `3-2/3-3/3-4` 继续(聚焦 FFmpeg `imdct_and_windowing` 同构状态机与 saved 表示法联动改造).

## Round-70 (3-2: LONG_START saved 最小探针回滚 + 状态机锚点补强)

- 子功能: `3-2`.
- 对比结论: `不一致(探针退化), 单测补强有效`.
- 逻辑证据:
  - 最小探针: 仅修改 `LONG_START` 的 `saved` 更新为“前 512 保留, 后段清零”.
  - P0 立即显著退化:
    - `data/1.m4a`: `99.883833% -> 99.798113%`, `max_err 0.476204097 -> 0.856235653`.
    - `data/2.m4a`: `99.999998% -> 99.833496%`, `max_err 0.000196815 -> 0.707782708`.
  - 结论与 Round-66 一致: 在当前 `windowed+saved` 表示法下, 仅改 LONG_START saved 区间会破坏 short/long 过渡链路.
- 修复改动:
  - `crates/tao-codec/src/decoders/aac/mod.rs`:
    - LONG_START 最小探针已完整回滚.
  - `crates/tao-codec/src/decoders/aac/tests.rs`:
    - 新增 `test_overlap_add_with_sequence_short_to_long_start_uses_transition_branch`.
    - 新增 `test_overlap_add_with_sequence_long_start_saved_updates_full_tail`.
- 验证命令与结果:
  - `cargo test -p tao-codec decoders::aac::tests::test_overlap_add_with_sequence -- --nocapture` -> `6 passed`.
  - `cargo test -p tao-codec decoders::aac -- --nocapture` -> `39 passed`.
  - `TAO_AAC_COMPARE_INPUT=data/1.m4a ... test_aac_compare`:
    - 回滚后恢复 `99.883833%`, `max_err=0.476204097`, `lag=0`.
  - `TAO_AAC_COMPARE_INPUT=data/2.m4a ... test_aac_compare`:
    - 回滚后恢复 `99.999998%`, `max_err=0.000196815`, `lag=0`.
- P0 指标变化:
  - 探针阶段显著退化, 回滚后恢复基线.
- P1 指标变化: `本轮未执行全量`.
- 判定: `无效修复(回滚), 但状态机回归锚点增强`.
- 下一子功能: `3-2/3-3/3-4` 继续(转向“先实现 FFmpeg vector_fmul_window 参考测试, 再做局部分段替换”).

## Round-71 (4-5/4-6: Pulse/TNS 参考对齐单测补齐)

- 子功能: `4-5`, `4-6`.
- 对比结论: `一致(测试锚点补强, 主路径持平)`.
- 逻辑证据:
  - 在 `spectral.rs` 测试模块补齐 Pulse/TNS 参考实现对照:
    - `apply_pulse_data` 与参考公式逐点对齐.
    - `compute_tns_lpc` 与参考递推逐点对齐.
    - `apply_tns_data` 与参考滤波逐点对齐.
  - 新增 overlap 状态机锚点 2 条继续保持通过.
- 修复改动:
  - `crates/tao-codec/src/decoders/aac/spectral.rs`:
    - 新增 `test_apply_pulse_data_matches_reference`.
    - 新增 `test_compute_tns_lpc_matches_reference`.
    - 新增 `test_apply_tns_data_matches_reference`.
    - 新增测试辅助参考函数 `apply_pulse_reference`/`compute_tns_lpc_reference`/`apply_tns_reference`.
  - `crates/tao-codec/src/decoders/aac/tests.rs`:
    - 保持 Round-70 新增 2 条 overlap 状态机锚点.
- 验证命令与结果:
  - `cargo test -p tao-codec decoders::aac::spectral::tests -- --nocapture` -> `8 passed`.
  - `cargo test -p tao-codec decoders::aac -- --nocapture` -> `42 passed`.
  - `TAO_AAC_COMPARE_INPUT=data/1.m4a ... test_aac_compare`:
    - `99.883833%`, `max_err=0.476204097`, `lag=0` (持平).
  - `TAO_AAC_COMPARE_INPUT=data/2.m4a ... test_aac_compare`:
    - `99.999998%`, `max_err=0.000196815`, `lag=0` (持平).
- P0 指标变化:
  - 双样本持平.
- P1 指标变化: `本轮未执行全量`.
- 判定: `有效修复(4-5/4-6 回归锚点建立, 无回归)`.
- 下一子功能: `3-2/3-3/3-4` 继续(转向 overlap 状态机与 FFmpeg `vector_fmul_window` 的分段同构实现).
